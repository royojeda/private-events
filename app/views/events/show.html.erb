<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <% if user_signed_in? %>
    <%# <%= button_to "Attend this event", attendances_path, params: { event: @event  } %>
    <%= form_with model: Attendance.new do |form| %>
      <%= hidden_field_tag "attendance[attended_event_id]", @event.id %>
      <%= form.submit "Attend this event" %>
    <% end %>
    <br><br>
  <% end %>
  <%# <%= link_to "Edit this event", edit_event_path(@event) %>
  <%= link_to "Back to events", events_path %>
  <br><br>
  <%# <%= button_to "Destroy this event", @event, method: :delete %>
</div>
